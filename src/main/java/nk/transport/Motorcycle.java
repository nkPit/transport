package nk.transport;

public class Motorcycle extends Transport{

    Motorcycle(String typeTransport, int numberOfWheel, int maxSpeed, String about) {
        super(typeTransport, numberOfWheel, maxSpeed, about);
    }
    
}
