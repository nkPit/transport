package nk.transport;

public class Scooter extends Transport{

    Scooter(String typeTransport, int numberOfWheel, int maxSpeed, String about) {
        super(typeTransport, numberOfWheel, maxSpeed, about);
    }
    
}
