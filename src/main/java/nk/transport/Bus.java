package nk.transport;

public class Bus extends Transport {
    Bus(String typeTransport, int numberOfWheel, int maxSpeed, String about) {
        super(typeTransport, numberOfWheel, maxSpeed, about);
    }
}